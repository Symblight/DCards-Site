let initialState = {

};

const reducerAuth = (state = initialState, action) => {
    switch(action.type) {
        default: {
            return state;
        }
    }
};

export default reducerAuth;